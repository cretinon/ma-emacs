:PROPERTIES:
:GPTEL_BACKEND: Gemini
:GPTEL_MODEL: gemini-3.5-flash
:GPTEL_SYSTEM: You are a large language model living in Emacs and a helpful assistant. Respond concisely. Always add a link to documentation.
:GPTEL_BOUNDS: ((response (1127 1833) (1851 2155) (2300 2324)))
:END:

* About
This file is used to :
- Easily save LLM parameters
- Check configuration
- Have conversation with LLM
  - Open this file in emacs
  - start a new conversation (at the end of the file near ***)
  - =C-c RET= once ready to send request

* Pre requisite
** Check configuration
In order to check backend, type C-c C-c inside code below
#+BEGIN_SRC emacs-lisp
  (message "backend=%s model=%s system=%s"
           (and (boundp 'gptel-backend) gptel-backend)
           (and (boundp 'gptel-model) gptel-model)
           (and (boundp 'gptel-directives) (alist-get 'default gptel-directives)))
#+END_SRC

** How to change/save configuration
- run =M-x gptel-menu=
- change config
- move cursor to =PROPERTIES=
- run =M-x gptel-org-set-properties=
- run a test query to validate
- save =C-x C-s=


* Conversation

***

hello


#+begin_reasoning
**Reviewing the Context*

I'm currently focused on dissecting this Emacs Org-mode setup. I've noted the =gptel= configuration within the file, which includes instructions and a code block for verification. I'm aiming to pinpoint the key areas for analysis.


*Crafting the Reply*

I've crafted a draft response to the "hello" greeting, designed for this specific Emacs and Org-mode context. It acknowledges the user, sets the scene for =gptel= interactions, and provides actionable guidance, including the command =C-c RET= for prompt submission. Crucially, I'm including a direct link to the =gptel= documentation within the response. I'm choosing Org-mode format for maximum compatibility and clarity.



#+end_reasoning

Hello! I am your AI assistant, running directly inside Emacs via =gptel=. 

How can I help you today? Feel free to type your question below and press =C-c RET= to send it.

For more information on configuring your environment, please refer to the [gptel documentation](https://github.com/karthink/gptel).

*** 

# Local Variables:
# eval: (remove-hook 'before-save-hook 'org-make-toc)
# eval: (gptel-mode 1)
# eval: (toggle-truncate-lines)
# eval: (org-toggle-inline-images)
# End:
